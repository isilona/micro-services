server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          #uri: https://github.com/isilona/micro-services/
          uri: file:C:/dev/git/micro-services
          search-paths:
          - configuration-properties
  application:
    name: configuration